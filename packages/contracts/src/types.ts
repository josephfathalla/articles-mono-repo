// API Contract Types
// Define your ORPC contract types and schemas here

// Example: Define contract schemas using @orpc/contract
// import { contract } from "@orpc/contract";
// import { z } from "zod";
//
// export const todoContract = contract({
//   getAll: {
//     input: z.void(),
//     output: z.array(z.object({ ... })),
//   },
//   create: {
//     input: z.object({ text: z.string().min(1) }),
//     output: z.object({ ... }),
//   },
// });

// For now, this file serves as a placeholder for your contract definitions.
// You can define contracts here and then use them in your api package.

// Export an empty object to make this a valid module
export {};
